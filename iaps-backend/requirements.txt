gunicorn==21.2.0
Flask==3.0.0
Flask-CORS==4.0.0
Flask-Mail==0.9.1
Flask-Limiter==3.5.0
PyJWT==2.8.0
pymongo==4.6.1
python-dotenv==1.0.0
bcrypt==4.1.2
dnspython==2.4.2
email-validator==2.1.0
Werkzeug==3.0.1
flask-socketio==5.3.6
redis==5.0.4
Pillow==10.4.0
openpyxl>=3.1.5

# AI Study Tools
# torch pulls in full GPU/CUDA support by default (~2GB of unused nvidia-cuda-*
# packages on a Render instance with no GPU) — the CPU-only wheel is a small
# fraction of the size and uses noticeably less memory just importing it.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
groq>=0.9.0
PyMuPDF>=1.24.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2

# Task queue (AI/RAG background work) — no-op until REDIS_URL is set
celery>=5.3.0

# Optional: only needed if S3_BUCKET is set (utils/storage.py). Not imported
# otherwise, so it's safe to leave uninstalled for a local-disk-only deployment.
# boto3>=1.34.0

# Testing
pytest>=9.0.0
pytest-flask>=1.3.0
mongomock>=4.3.0
